export type PaymentCondition = {
  id: string;
  name: string;
};

export type CfopOption = {
  id: string;
  code: string;
  description: string;
};

export type GeneralOption = {
  id: string;
  name: string;
};
